name: Build Nuxt
on: [push]
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Nuxt
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: ashkantaravati/NuxtToGithubPages@v1.0
      with:
        username: 'tokka2'
        reponame: 'nuxt-amazon-timesale'
        token: ${{ secrets.GITHUB_TOKEN }} # Leave this line unchanged
